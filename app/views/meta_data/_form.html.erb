<%= form_for(@meta_datum) do |f| %>
  <% if @meta_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meta_datum.errors.count, "error") %> prohibited this meta_datum from being saved:</h2>

      <ul>
      <% @meta_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :table_name %><br>
    <%= f.select :table_name, options_for_select(DYNAMIC_TABLES) %>
  </div>
  <div class="field">
    <%= f.label :field_name %><br>
    <%= f.text_field :human_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
